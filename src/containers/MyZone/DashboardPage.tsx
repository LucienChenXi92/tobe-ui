import { Box } from "@mui/material";
export default function DashboardPage() {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        width: { xs: "90%", md: "60%" },
      }}
    >
      Dashboard page
    </Box>
  );
}
